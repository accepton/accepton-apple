#Install via Carthage

# 1. Create a `Cartfile` to your project folder and add the following line

```ruby
github "accepton/accepton-apple" ~> 0.5
```

# 2. Run `carthage update`

# 3. Drag and drop from `Carthage/Build` into `Linked Frameworks and Libraries` 
